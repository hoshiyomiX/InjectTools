# THIS WORKFLOW IS DISABLED
# Replaced by: android-ci.yml
# Reason: New workflow has more features (caching, diagnostics, PR comments)
# Date: 2026-01-27

name: Android Debug APK Build (DISABLED)

on:
  # Disabled - no triggers
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled."
          echo "Please use 'Android CI' workflow instead."
          exit 1
