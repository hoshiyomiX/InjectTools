<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal theme for Jetpack Compose apps -->
    <!-- Most styling is handled in Compose Material3 theme -->
    <style name="Theme.InjectTools" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
